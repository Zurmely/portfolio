---
import ZTextLink from "@/components/zui/TextLink";

interface Props {
  href: string;
  external?: boolean;
  class?: string;
}

const { href, external = false, class: className }: Props = Astro.props;
---

<ZTextLink href={href} external={external} className={className}>
  <slot />
</ZTextLink>
