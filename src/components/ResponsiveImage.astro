---
interface Props {
  src?: string;
  alt: string;
  caption?: string;
  placeholderLabel?: string;
}

const {
  src,
  alt,
  caption,
  placeholderLabel = "TODO_CONTENT media placeholder",
}: Props = Astro.props;
---

<figure>
  {
    src ? (
      <img src={src} alt={alt} loading="lazy" decoding="async" />
    ) : (
      <div class="placeholder-media">{placeholderLabel}</div>
    )
  }
  {
    caption ? (
      <figcaption style="margin-top: var(--z-spacing-gap-inline); font-size: var(--z-text-caption-size); color: var(--z-color-text-tertiary);">
        {caption}
      </figcaption>
    ) : null
  }
</figure>
