---
import BreadcrumbsNav from "@/components/zui/BreadcrumbsNav";

interface Crumb {
  label: string;
  href?: string;
}

interface Props {
  items: Crumb[];
  label?: string;
}

const { items, label }: Props = Astro.props;
---

<BreadcrumbsNav items={items} label={label} />
